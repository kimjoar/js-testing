describe("The user view", function(){

    xit("should have a user when initialized", function(){
        var user = {};

        // ...

        // expect(view.user).toBeDefined();
    });

    xit("should show name when rendered", function(){
        var user = {
            name: "Test Testesen"
        };

        // ...
    });

    xit("should show name in heading when rendered", function() {
        var user = {
            name: "Test Testesen"
        };

        // ...
    });

    xit("should render model", function() {
        var user = new BEKK.User({ screen_name: "kimjoar" });

        // ...
    });

    xit("should fetch data from Twitter and populate the view", function(){
      // runs
      // waits
      // runs
    });

    xit("should populate the view when data is fetched", function() {
    });

    xit("should populate the view when data is fetched", function(){
    });

    xit("should update view when monolog is added", function() {
    });

});
