describe("the statuses view", function() {
  xit("should show all monologs", function() {
    var monologs = new BEKK.Monologs();
    monologs.attr("monologs", ["Status 1", "Status 2"]);

    // ...
  });

  xit("should update view when a monolog is added", function() {
  });
});
